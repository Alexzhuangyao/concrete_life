# 混凝土搅拌站管理系统 - 启动指南

## 🚀 快速启动

### Windows 系统
双击运行 `start.bat` 或在命令行中执行：
```cmd
start.bat
```

### Linux/Mac 系统
在终端中执行：
```bash
chmod +x start.sh
./start.sh
```

## 📋 前置要求

### 必需依赖
- **Node.js** (v18 或更高版本)
- **npm** (v9 或更高版本)

### 可选依赖
- **Docker** (用于运行 PostgreSQL 数据库)
- **Docker Compose** (用于管理容器)

## 🔧 启动脚本功能

启动脚本会自动执行以下操作：

1. ✅ 检查系统依赖（Node.js, npm）
2. ✅ 安装项目依赖（如果尚未安装）
3. ✅ 启动 PostgreSQL 数据库（如果安装了 Docker）
4. ✅ 启动后端服务（端口 3000）
5. ✅ 启动前端服务（端口 5173）
6. ✅ 显示服务地址和状态

## 🌐 服务地址

启动成功后，可以访问以下地址：

- **前端应用**: http://localhost:5173
- **后端 API**: http://localhost:3000
- **API 文档**: http://localhost:3000/api

## 🛑 停止服务

### Windows 系统
双击运行 `stop.bat` 或在命令行中执行：
```cmd
stop.bat
```

### Linux/Mac 系统
在终端中执行：
```bash
./stop.sh
```

## 📝 日志文件

服务运行日志保存在 `logs` 目录：
- `logs/backend.log` - 后端服务日志
- `logs/frontend.log` - 前端服务日志

## ⚙️ 手动启动（高级）

如果需要手动启动服务：

### 启动数据库
```bash
docker-compose up -d postgres
```

### 启动后端
```bash
cd concrete-plant-api
npm install
npm run start:dev
```

### 启动前端
```bash
cd concrete-plant-web
npm install
npm run dev
```

## 🐛 常见问题

### 1. 端口被占用
如果提示端口 3000 或 5173 被占用：
- Windows: 使用 `netstat -ano | findstr :3000` 查找占用进程
- Linux/Mac: 使用 `lsof -i :3000` 查找占用进程

### 2. 数据库连接失败
- 确保 Docker 已启动
- 检查 `docker-compose.yml` 配置
- 运行 `docker-compose logs postgres` 查看数据库日志

### 3. 依赖安装失败
- 清除缓存: `npm cache clean --force`
- 删除 `node_modules` 和 `package-lock.json`
- 重新运行启动脚本

### 4. 权限问题（Linux/Mac）
如果脚本无法执行：
```bash
chmod +x start.sh stop.sh
```

## 📦 项目结构

```
concrete_life/
├── start.sh              # Linux/Mac 启动脚本
├── start.bat             # Windows 启动脚本
├── stop.sh               # Linux/Mac 停止脚本
├── stop.bat              # Windows 停止脚本
├── docker-compose.yml    # Docker 配置
├── concrete-plant-api/   # 后端项目
├── concrete-plant-web/   # 前端项目
└── logs/                 # 日志目录
```

## 🔐 默认账号

首次启动后，可以使用以下账号登录：
- 用户名: `admin`
- 密码: `admin123`

（请在生产环境中修改默认密码）

## 📞 技术支持

如有问题，请查看：
- 项目文档
- 日志文件
- GitHub Issues

---

**版本**: 1.0.0  
**更新时间**: 2026-01-28

